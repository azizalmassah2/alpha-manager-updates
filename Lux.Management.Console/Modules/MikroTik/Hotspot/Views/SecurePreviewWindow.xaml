<Window x:Class="Lux.Management.Console.Modules.MikroTik.Hotspot.Views.SecurePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="معاينة صفحة تسجيل الدخول الآمنة" 
        Height="800" Width="1200" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Status Bar -->
        <Border Grid.Row="0" Background="#f6f8fa" BorderBrush="#e1e4e8" BorderThickness="0,0,0,1" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="🔒 نمط المعاينة الآمنة (الذاكرة فقط)" FontWeight="Bold" Foreground="#24292e" FontSize="14" Margin="0,0,12,0"/>
                    <TextBlock Text="— تم قفل عرض المصدر ونسخ الملفات لحماية القالب من التوزيع غير المصرح به." Foreground="#586069" VerticalAlignment="Center" FontSize="12"/>
                </StackPanel>
                <Button Grid.Column="1" Content="إغلاق المعاينة" Click="Close_Click" Padding="16,6" Background="#dc3545" Foreground="White" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <!-- WebView2 Control -->
        <wv2:WebView2 x:Name="webView" Grid.Row="1" />
    </Grid>
</Window>
