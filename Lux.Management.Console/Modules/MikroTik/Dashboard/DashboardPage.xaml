<UserControl x:Class="Lux.Management.Console.Modules.MikroTik.Dashboard.DashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Dashboard Title / Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="لوحة تحكم العمليات والمراقبة" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                <TextBlock Text="مراقبة حية وشاملة لجميع العمليات والأجهزة المتصلة بالنظام" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Stats Grid (Responsive Flow) -->
            <UniformGrid Grid.Row="1" Margin="0,0,0,24" Columns="4" VerticalAlignment="Top">
                
                <!-- Total Devices Card -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,16,16" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="إجمالي كروت المشتركين" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding TotalDevices}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="&#xE8F1;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.8"/>
                    </Grid>
                </Border>

                <!-- Online Devices Card -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,16,16" BorderBrush="{DynamicResource SuccessBrush}" BorderThickness="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="المشتركون النشطون" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding OnlineDevices}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{DynamicResource SuccessBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.8"/>
                    </Grid>
                </Border>

                <!-- Offline Devices Card -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,16,16" BorderBrush="{DynamicResource MutedTextBrush}" BorderThickness="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="كروت المشتركين المنتهية" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding OfflineDevices}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="&#xE71A;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.8"/>
                    </Grid>
                </Border>

                <!-- Warning Devices Card -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,16,16" BorderBrush="{DynamicResource WarningBrush}" BorderThickness="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="الحسابات الموقوفة مؤقتاً" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding WarningDevices}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource WarningBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{DynamicResource WarningBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.8"/>
                    </Grid>
                </Border>

                <!-- Critical Devices Card -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,16,16" BorderBrush="{DynamicResource ErrorBrush}" BorderThickness="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="كروت المخزون غير المفعلة" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding CriticalDevices}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource ErrorBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="&#xE730;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{DynamicResource ErrorBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.8"/>
                    </Grid>
                </Border>

                <!-- Total Projects Card -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,16,16" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="باقات المشتركين المتاحة" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding TotalProjects}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="&#xE909;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.8"/>
                    </Grid>
                </Border>

                <!-- Active Sessions Card -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,16,16" BorderBrush="{DynamicResource InfoBrush}" BorderThickness="0,0,0,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="جلسات Hotspot النشطة" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding ActiveMonitoringSessions}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource InfoBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="&#xEA39;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{DynamicResource InfoBrush}" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.8"/>
                    </Grid>
                </Border>

            </UniformGrid>

            <!-- Bottom Content: Alerts and Health -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Recent Alerts Panel -->
                <Border Style="{StaticResource ModernCardStyle}" Grid.Column="0" VerticalAlignment="Top">
                    <StackPanel>
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="أحدث التنبيهات والتحذيرات" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="سجل التنبيهات المباشرة لنشاط الأجهزة" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Alerts Items List -->
                        <ItemsControl ItemsSource="{Binding RecentAlerts}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="12,10" Background="Transparent">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Alert Icon Indicator -->
                                            <Ellipse Grid.Column="0" Width="8" Height="8" Fill="{DynamicResource WarningBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            
                                            <!-- Severity Badge -->
                                            <TextBlock Grid.Column="1" Text="{Binding Severity}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource WarningBrush}" VerticalAlignment="Center"/>
                                            
                                            <!-- Alert Message -->
                                            <TextBlock Grid.Column="2" Text="{Binding Message}" FontSize="13" Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Margin="10,0"/>
                                            
                                            <!-- Timestamp -->
                                            <TextBlock Grid.Column="3" Text="{Binding Timestamp, StringFormat={}{0:HH:mm}}" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Empty State if no alerts -->
                        <TextBlock Text="لا توجد تنبيهات نشطة حالياً" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" Margin="0,20,0,10">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RecentAlerts.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                    </StackPanel>
                </Border>

            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
