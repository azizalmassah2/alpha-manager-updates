<UserControl x:Class="Lux.Management.Console.Modules.Monitoring.DevicesMonitorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="مراقبة الأجهزة" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
            <TextBlock Text="حالة المراقبة الحية للأجهزة المسجلة بالنظام" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Sessions}" AutoGenerateColumns="False" HeadersVisibility="All" Background="Transparent" BorderThickness="0" RowHeight="50" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="معرف الجهاز" Binding="{Binding DeviceId}" Width="250" />
                <DataGridTextColumn Header="الحالة" Binding="{Binding CurrentStatus}" Width="100" />
                <DataGridTextColumn Header="وقت البدء" Binding="{Binding StartTime, StringFormat={}{0:g}}" Width="150" />
                <DataGridTextColumn Header="آخر قراءة" Binding="{Binding LastReadTime, StringFormat={}{0:g}}" Width="150" />
                <DataGridTextColumn Header="رسالة الخطأ" Binding="{Binding ErrorMessage}" Width="*" />
                
                <DataGridTemplateColumn Header="إجراءات" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="إيقاف" Style="{StaticResource ButtonDanger}" Command="{Binding DataContext.StopMonitoringCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
