<UserControl x:Class="Lux.Management.Console.Modules.Broadcasting.Views.BroadcastingFlashingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:Lux.Management.Console.Modules.Broadcasting.QuickConfig.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisInverted" Invert="True"/>
        <helpers:InverseBoolConverter x:Key="InverseBool"/>

        <Style x:Key="FlashBtnStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.88"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource RegionBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="340"/>  <!-- Left: Device list -->
            <ColumnDefinition Width="1"/>    <!-- Divider -->
            <ColumnDefinition Width="*"/>    <!-- Right: Flash settings -->
        </Grid.ColumnDefinitions>

        <!-- ════════ LEFT: Device List ════════ -->
        <Border Grid.Column="0"
                Background="{DynamicResource SidebarBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="16,14,16,10"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                    <StackPanel>
                        <TextBlock Text="💾  موديلات الأجهزة" FontSize="14" FontWeight="Bold"
                                   Foreground="{DynamicResource PrimaryTextBrush}"/>
                        <TextBlock Text="اختر الجهاز من القائمة أدناه"
                                   FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding FirmwareDevices}"
                         SelectedItem="{Binding SelectedFirmwareDevice, Mode=TwoWay}"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Border Padding="16,8,16,4">
                                        <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="Bold"
                                                   Foreground="{DynamicResource PrimaryBrush}"
                                                   Opacity="0.85"/>
                                    </Border>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListBox.GroupStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,10" CornerRadius="6" Margin="6,2">
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{DynamicResource PrimaryTextBrush}"/>
                                    <TextBlock Text="{Binding Notes}" FontSize="11"
                                               Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Bd" Background="Transparent" CornerRadius="6" Margin="4,2">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background">
                                                    <Setter.Value>
                                                        <SolidColorBrush Color="{DynamicResource PrimaryColor}" Opacity="0.12"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <!-- Divider -->
        <Border Grid.Column="1" Background="{DynamicResource BorderBrush}"/>

        <!-- ════════ RIGHT: Flash Settings ════════ -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">

                <!-- Title card -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="⚙️  إعدادات التفليش" FontSize="16" FontWeight="Bold"
                                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,6"/>
                        <TextBlock FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}">
                            اختر الجهاز من القائمة، حدد مصدر الفيرموير، وأدخل عنوان IP الجهاز للبدء.
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Target IP -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="عنوان IP الجهاز المستهدف" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,8"/>
                        <TextBox Text="{Binding TargetIp, UpdateSourceTrigger=PropertyChanged}"
                                 Height="34" Padding="12,6" FontSize="14"
                                 Background="{DynamicResource SurfaceBrush}"
                                 Foreground="{DynamicResource PrimaryTextBrush}"
                                 BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                    </StackPanel>
                </Border>

                <!-- Firmware source selector -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="مصدر الفيرموير" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,12"/>

                        <!-- Local File -->
                        <RadioButton Content="ملف محلي (من الكمبيوتر)"
                                     IsChecked="{Binding UseServerSource, Converter={StaticResource InverseBool}, Mode=TwoWay}"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     FontSize="13" Margin="0,0,0,10"/>

                        <Grid Visibility="{Binding UseServerSource, Converter={StaticResource BoolToVisInverted}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding SelectedFirmwarePath}"
                                     IsReadOnly="True"
                                     Height="34" Padding="12,6" FontSize="12"
                                     Background="{DynamicResource SurfaceBrush}"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                            <Button Grid.Column="1"
                                    Command="{Binding BrowseLocalFileCommand}"
                                    Content="📂  تصفح..."
                                    Background="{DynamicResource PrimaryBrush}" Foreground="White"
                                    Style="{StaticResource FlashBtnStyle}"
                                    Margin="8,0,0,0"/>
                        </Grid>

                        <!-- Separator -->
                        <Separator Margin="0,16" Background="{DynamicResource BorderBrush}"/>

                        <!-- Server source -->
                        <RadioButton Content="تنزيل من السيرفر"
                                     IsChecked="{Binding UseServerSource, Mode=TwoWay}"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     FontSize="13" Margin="0,0,0,10"/>
                        <TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding UseServerSource}"
                                 Height="34" Padding="12,6" FontSize="13"
                                 Background="{DynamicResource SurfaceBrush}"
                                 Foreground="{DynamicResource PrimaryTextBrush}"
                                 BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>
                    </StackPanel>
                </Border>

                <!-- Status + Flash button -->
                <Border Style="{StaticResource SectionCard}">
                    <StackPanel>
                        <TextBlock Text="{Binding StatusMessage}"
                                   FontSize="13" Foreground="{DynamicResource SecondaryTextBrush}"
                                   TextWrapping="Wrap" Margin="0,0,0,16"/>
                        <Button Content="⚡  بدء التفليش"
                                Background="#f59e0b" Foreground="White"
                                Style="{StaticResource FlashBtnStyle}"
                                HorizontalAlignment="Left"
                                IsEnabled="{Binding IsFlashing, Converter={StaticResource InverseBool}}"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
