<UserControl x:Class="Lux.Management.Console.Core.Views.ActiveRouterStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <Style x:Key="StateToBrushConverter" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource RegionBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding State}" Value="Connected">
                    <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Disconnected">
                    <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Connecting">
                    <Setter Property="Background" Value="{DynamicResource WarningBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Switching">
                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="AuthenticationFailed">
                    <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Timeout">
                    <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Error">
                    <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <!-- Status Indicator -->
        <Border Width="10" Height="10" CornerRadius="5" Margin="0,0,8,0" Style="{StaticResource StateToBrushConverter}" />
        
        <!-- Status Text -->
        <TextBlock Text="{Binding State}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" />
        
        <!-- Router Name -->
        <TextBlock Text="{Binding ActiveRouterName}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" />
        
        <!-- Host/IP (Optional) -->
        <TextBlock Text="{Binding Host, StringFormat='({0})'}" VerticalAlignment="Center" Margin="8,0,0,0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Host}" Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </StackPanel>
</UserControl>
