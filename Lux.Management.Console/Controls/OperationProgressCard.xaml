<UserControl x:Class="Lux.Management.Console.Controls.OperationProgressCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="400"
             Background="{DynamicResource SurfaceBrush}"
             BorderBrush="{DynamicResource BorderBrush}"
             BorderThickness="1"
             Padding="10">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Text="{Binding OperationName}" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource TextBrush}" />
        
        <ProgressBar Grid.Row="1" Value="{Binding ProgressPercentage}" Height="10" Margin="0,10" Foreground="{DynamicResource PrimaryBrush}" Background="{DynamicResource BorderBrush}" />
        
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="{Binding CompletedDevices, StringFormat='مكتمل: {0}'}" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding FailedDevices, StringFormat='فاشل: {0}'}" Foreground="{DynamicResource ErrorBrush}" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding RemainingDevices, StringFormat='متبقي: {0}'}" Foreground="{DynamicResource SecondaryTextBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
