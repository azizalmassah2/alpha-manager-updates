<UserControl x:Class="Lux.Management.Console.Controls.DeviceCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300"
             Background="{DynamicResource SurfaceBrush}"
             BorderBrush="{DynamicResource BorderBrush}"
             BorderThickness="1"
             Padding="10">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding DeviceName}" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource TextBrush}" />
        
        <TextBlock Grid.Row="1" Text="{Binding Vendor}" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
        
        <TextBlock Grid.Row="2" Text="{Binding IpAddress}" FlowDirection="LeftToRight" HorizontalAlignment="Left" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,5,0,0"/>
    </Grid>
</UserControl>
