
            <!-- LEFT: Card Dimensions + Margins + Frame + Columns/Rows -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="10" DataContext="{Binding SelectedTemplate}">
                <StackPanel>
                    <TextBlock Text="أبعاد الكرت" Style="{StaticResource SectionTitleStyle}" FontSize="13"/>

                    <!-- Rows = Card Height -->
                    <TextBlock Text="طول الكرت (صفوف)" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,2"/>
                    <Border BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="-" Click="BtnRowDown_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                            <TextBox Grid.Column="1" Text="{Binding Rows}" IsReadOnly="True" TextAlignment="Center" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12"/>
                            <Button Grid.Column="2" Content="+" Click="BtnRowUp_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                        </Grid>
                    </Border>

                    <!-- Columns = Card Width -->
                    <TextBlock Text="عرض الكرت (أعمدة)" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,2"/>
                    <Border BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,12">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="-" Click="BtnColDown_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                            <TextBox Grid.Column="1" Text="{Binding Columns}" IsReadOnly="True" TextAlignment="Center" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12"/>
                            <Button Grid.Column="2" Content="+" Click="BtnColUp_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                        </Grid>
                    </Border>

                    <Separator Margin="0,0,0,10"/>
                    <TextBlock Text="هامش الكرت" Style="{StaticResource SectionTitleStyle}" FontSize="13"/>

                    <TextBlock Text="أفقي :" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,2"/>
                    <Border BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="-" Click="BtnMarginXDown_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                            <TextBox Grid.Column="1" Text="{Binding MarginX}" IsReadOnly="True" TextAlignment="Center" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12"/>
                            <Button Grid.Column="2" Content="+" Click="BtnMarginXUp_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                        </Grid>
                    </Border>

                    <TextBlock Text="رأسي :" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,2"/>
                    <Border BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,0,12">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="-" Click="BtnMarginYDown_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                            <TextBox Grid.Column="1" Text="{Binding MarginY}" IsReadOnly="True" TextAlignment="Center" BorderThickness="0" Background="Transparent" VerticalAlignment="Center" FontSize="12"/>
                            <Button Grid.Column="2" Content="+" Click="BtnMarginYUp_Click" Background="{DynamicResource BgMediumBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand" Padding="8,4"/>
                        </Grid>
                    </Border>

                    <Separator Margin="0,0,0,10"/>
                    <TextBlock Text="إطار الكرت" Style="{StaticResource SectionTitleStyle}" FontSize="13"/>

                    <TextBlock Text="حجم الإطار :" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding FrameSize}" Style="{StaticResource ModernTextBoxStyle}" FontSize="11" Padding="4" Margin="0,0,0,8"/>

                    <TextBlock Text="لون الإطار :" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding FrameColorHex}" Style="{StaticResource ModernTextBoxStyle}" FontSize="11" Padding="4"/>
                </StackPanel>
            </Border>

        </Grid>
