<UserControl x:Class="MikroTikVoucherPrinter.UI.Views.Pages.TemplateManagementPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:MikroTikVoucherPrinter.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1500"
             FlowDirection="RightToLeft"
             Background="{DynamicResource BgDarkBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:MathConverter x:Key="MathConverter"/>
        <converters:DoubleToThicknessConverter x:Key="DoubleToThicknessConverter"/>
        <converters:XYToMarginConverter x:Key="XYToMarginConverter"/>
        <converters:CardSizeConverter x:Key="CardSizeConverter"/>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Template Selection Bar -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="10,8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}" DisplayMemberPath="Name" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Margin="0,0,16,0" MinHeight="32"/>
                <Button Grid.Column="1" Content="قالب جديد" Command="{Binding AddNewCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="4,0" Padding="16,4" MinHeight="32"/>
                <Button Grid.Column="2" Content="حفظ" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="4,0" Padding="16,4" MinHeight="32"/>
                <Button Grid.Column="3" Content="حذف" Command="{Binding DeleteCommand}" Style="{StaticResource SecondaryButtonStyle}" Foreground="#F87171" BorderBrush="#F87171" Margin="4,0,0,0" Padding="16,4" MinHeight="32"/>
            </Grid>
        </Border>

        <!-- Row 1: Basic Template Data and Images -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="10" Margin="0,0,0,10" DataContext="{Binding SelectedTemplate}" Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Height="32" Margin="0,0,8,0" Padding="8,4" Tag="اسم القالب"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding DataContext.AvailableProfiles, RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedItem="{Binding LinkedProfileName}" Style="{StaticResource MaterialDesignFloatingHintComboBox}" Height="32" Margin="0,0,8,0" Tag="باقة المايكروتيك (اختياري)"/>
                <CheckBox Grid.Column="2" Content="قالب افتراضي" IsChecked="{Binding IsDefault}" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                <Grid Grid.Column="4" Margin="0,0,8,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding BackgroundImagePath}" IsReadOnly="True" Style="{StaticResource ModernTextBoxStyle}" Height="32" Padding="8,4" VerticalContentAlignment="Center" Tag="مسار الخلفية"/>
                    <Button Grid.Column="1" Content="خلفية" Command="{Binding DataContext.BrowseBackgroundCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource SecondaryButtonStyle}" Margin="4,0,0,0" Height="32" Padding="8,0"/>
                </Grid>
                <Grid Grid.Column="5">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding LogoImagePath}" IsReadOnly="True" Style="{StaticResource ModernTextBoxStyle}" Height="32" Padding="8,4" VerticalContentAlignment="Center" Tag="مسار الشعار"/>
                    <Button Grid.Column="1" Content="شعار" Command="{Binding DataContext.BrowseLogoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource SecondaryButtonStyle}" Margin="4,0,0,0" Height="32" Padding="8,0"/>
                </Grid>
            </Grid>
        </Border>
